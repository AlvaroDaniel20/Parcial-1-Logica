import java.util.Scanner;

public class contarNumerosPrimos {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        boolean opcion = true;

        while (opcion) {
            System.out.println("Ingrese dos números enteros positivos: ");
            int numero1 = scanner.nextInt();
            int numero2 = scanner.nextInt();
            if () {
                int primos1 = numero1 / 1;
                int Primos1 = numero1 / numero1;
                int primos2 = numero2 / 1;
                int Primos2 = numero2 / numero2;
        } 
            

            System.out.println("Entrada: " + numero1 + ", " + numero2 + "// " + "(" + primos1 + primos2 + ")");
        
        
        }
    }
}